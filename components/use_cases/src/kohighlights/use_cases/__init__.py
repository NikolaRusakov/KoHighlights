"""
Use cases — the orchestration layer.

Each module exposes one or more plain functions that take domain models and
infrastructure objects as arguments and return domain models or raise
well-typed exceptions.  No UI imports are allowed here.
"""
